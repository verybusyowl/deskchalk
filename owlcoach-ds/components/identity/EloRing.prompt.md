Identity hero ring — ELO arc sweeps in and the number counts up on mount. Orange because ELO is "heat". Sits in the identity strip, not the focus hero.

```jsx
<EloRing elo={1350} level={6} progress={0.52} />
```

`progress` is the fraction through the current level band. Respects reduced-motion.
